# =====================================================================
# 轴承表面缺陷智能检测系统 —— Python 依赖清单
# 环境：Windows 11 + Python 3.13（项目根目录 venv 虚拟环境）
#
# 说明：torch / torchvision 不在本清单中（见文件末尾「GPU 版安装说明」），
#       请先安装本清单，再按显卡 CUDA 版本单独安装 PyTorch GPU 版。
#
# 完整安装步骤（venv 激活状态下执行）：
#   1) pip install -r requirements.txt
#   2) pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   3) python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
#      应输出 2.x.x+cu128 和 True
# =====================================================================

# ---------- 深度学习：YOLOv8（算法方向一：计算机视觉 / 缺陷区域定位） ----------
ultralytics>=8.3.0

# ---------- 机器学习：SVM（算法方向二：机器学习 / 二级判别） ----------
scikit-learn>=1.5.0

# ---------- Web 后端：FastAPI ----------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0     # ASGI 服务器
python-multipart>=0.0.9       # 文件上传解析
pydantic>=2.8.0
pydantic-settings>=2.4.0      # 从 .env 读取配置
python-dotenv>=1.0.1

# ---------- 数据库与缓存 ----------
sqlalchemy>=2.0.30            # ORM 框架
psycopg2-binary>=2.9.10       # PostgreSQL 驱动
redis>=5.0.1                  # Redis 客户端

# ---------- 图像处理与科学计算 ----------
opencv-python>=4.10.0         # 图像读写 / 预处理 / HOG 特征
pillow>=10.4.0
numpy>=1.26.0
scipy>=1.14.0
pandas>=2.2.0
matplotlib>=3.9.0             # EDA / 收敛曲线 / PR 曲线绘图

# ---------- 工具 ----------
pyyaml>=6.0                   # YOLO 数据集配置文件
tqdm>=4.66.0                  # 进度条

# ---------- 测试 ----------
pytest>=8.2.0
httpx>=0.27.0                 # FastAPI TestClient 依赖

# =====================================================================
# PyTorch GPU 版单独安装说明
# 本机显卡：RTX 4060（8GB） / 驱动 571.96 / 支持 CUDA 12.8
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# 若官方源下载慢，可用阿里云镜像：
#   pip install torch torchvision --index-url https://mirrors.aliyun.com/pytorch-wheels/cu128
# =====================================================================
