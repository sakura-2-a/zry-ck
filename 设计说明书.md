# 基于深度学习的轴承表面缺陷智能检测系统

## 设计说明书

| 项目 | 内容 |
| 课程名称 | 《制造智能技术》课程设计 |
| 设计题目 | 基于深度学习的轴承表面缺陷智能检测系统 |
| 学生姓名 | 张芮源 |
| 学　　号 |202412020125 |
| 指导教师 | 董旭彬 |
| 完成日期 | 2026 年 9 月 |

> **数据来源说明**：本系统采用的 KolektorSDD 为公开的工业表面缺陷数据集
---

## 目录

1. 背景调研与研究意义
2. 方案设计
3. 数据集构建
4. 三个技术方向的详细设计
5. 界面设计
6. 调试
7. 总结与展望

---

## 1 背景调研与研究意义

### 1.1 课题背景

轴承是机械装备中最关键的支承与传动基础件之一，被誉为"工业的关节"。轴承质量直接决定整机设备的运转精度、服役寿命与可靠性：据相关统计，旋转机械故障中约有 30%~50% 与轴承有关。轴承表面缺陷是轴承失效的重要诱因——表面划痕会在交变载荷下扩展为疲劳裂纹，最终导致设备停机甚至安全事故。因此，轴承出厂前的表面缺陷检测是制造环节中必不可少的一道质量关卡。

### 1.2 传统检测方法的局限

当前产线上主流的表面缺陷检测仍以**人工目检**为主，其固有缺陷十分明显：

1. **效率低**：目检速度远低于产线节拍，往往需要安排多班次人员；
2. **主观性强**：不同检验员的判断标准不统一，同一检验员的状态波动也会造成漏检、误检；
3. **漏检率高**：长时间注视高反光金属表面易产生视觉疲劳，微小划痕极易漏过。

传统机器视觉方法虽然实现了自动化，但依赖人工设计的特征与固定阈值，对光照变化、纹理干扰敏感，泛化能力弱，难以适应缺陷形态多变、背景复杂的真实产线场景。

### 1.3 深度学习在表面缺陷检测中的研究现状

深度学习通过数据驱动的方式自动学习缺陷的高层语义特征，在表面缺陷检测领域已逐步取代传统方法，成为研究主流：

- **分类方法**：以卷积神经网络（CNN）判断整幅图像是否存在缺陷，结构简单，但无法给出缺陷位置；
- **目标检测方法**：YOLO 系列等单阶段检测器可直接输出缺陷的边界框，兼顾速度与精度，是工业在线检测的主流选择；缺点是模型置信度标定依赖充足的训练数据，小数据集下置信度普遍偏低；
- **分割方法**：Tabernik 等人（2020）提出"分割网络 + 决策网络"的两阶段方案，先分割缺陷区域、再对分割结果做图像级判别，在 KolektorSDD 数据集上取得领先效果，说明"粗定位 + 精判别"的级联思想在弱标注小样本场景下十分有效。

上述级联思想正是本课题方案设计的出发点。

### 1.4 数据集来源：KolektorSDD

本课题采用斯洛文尼亚卢布尔雅那大学（University of Ljubljana）Vicos 实验室公开发布的 **KolektorSDD（Kolektor Surface-Defect Dataset）** 表面缺陷数据集（[https://www.vicos.si/Downloads/KolektorSDD](https://www.vicos.si/Downloads/KolektorSDD)）。

该数据集在受控光照条件下采集，共包含 **399 张灰度图像**（约 500×1258 像素，单通道），来自 **50 个电子换向器物理件**（每个件采集约 8 张不重叠的表面视图），其中 **52 张含缺陷、347 张无缺陷**。数据集提供**像素级掩码标注**（`*_label.bmp`，白色区域为缺陷），官方划分了 Train / Test 两个子集，是表面缺陷检测领域常用的基准数据集。

---

## 2 方案设计

### 2.1 需求分析

#### 2.1.1 功能性需求

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-1 | 图像上传检测 | 通过 Web 页面上传单张表面图像，系统调用检测流水线（YOLOv8 定位 + SVM 判别），返回缺陷判定结果、缺陷框坐标、置信度与处理耗时 |
| FR-2 | 结果可视化 | 前端在图片上叠加渲染缺陷框与标签，展示整体判定（有缺陷/无缺陷）、缺陷数量、平均置信度 |
| FR-3 | 历史记录 | 每次检测自动持久化到 PostgreSQL；支持分页查询、按判定结果筛选、查看详情、删除 |
| FR-4 | 统计看板 | 展示累计检测数、缺陷率、近 7 天检测趋势、缺陷数分布等统计图表 |
| FR-5 | 模型信息 | 展示 YOLOv8 / SVM / GA 三个模型的版本、评估指标、GA 最优超参数与收敛曲线 |
| FR-6 | 离线训练与优化 | 提供 YOLOv8 训练、特征提取与 SVM 训练、GA 超参优化三个命令行脚本，输出权重文件、评估报告、最优参数并写入数据库 |

#### 2.1.2 非功能性需求

| 编号 | 需求 | 说明 |
|---|---|---|
| NFR-1 | 可运行性 | 整条链路在本机均可跑通；训练脚本自动检测 GPU/CPU |
| NFR-2 | 三方向覆盖 | YOLOv8（计算机视觉）、SVM（机器学习）、GA（智能优化）均承担不可替代的实际职责 |
| NFR-3 | 代码质量 | 全部代码中文注释；关键函数带类型注解与文档字符串；模块化分层 |
| NFR-4 | 可部署性 | 提供 Dockerfile 与 docker-compose，一键启动后端 + 前端 + PostgreSQL + Redis |
| NFR-5 | 可追溯性 | 开发过程按模块记录设计思路与关键决策（附录 A：AI 使用记录） |

### 2.2 技术选型与理由

| 技术 | 角色 | 选型理由 |
|---|---|---|
| YOLOv8（ultralytics） | 缺陷区域定位 | 主流单阶段检测器，训练部署简单，与 FastAPI 集成方便 |
| SVM（scikit-learn） | 二级判别 | 小样本高维特征下稳健；与深度学习形成"传统 ML + 深度 CV"互补，契合课程方向要求 |
| 遗传算法（手写实现） | 超参数优化 | 完整展示选择/交叉/变异算子，过程可控可解释，便于撰写报告 |
| FastAPI | 后端框架 | 高性能异步框架，自带 Swagger 文档，与算法栈同语言 |
| Vue3 + Vite + Element Plus + ECharts | 前端 | 组件化开发效率高，Element Plus 上手快，ECharts 满足统计图表需求 |
| PostgreSQL + SQLAlchemy 2 | 数据持久化 | 检测记录/模型元数据为关系型数据；ORM 便于维护与测试 |
| Redis | 缓存 | 缓存近期检测结果与统计汇总，体现缓存层设计 |
| Docker | 部署 | 交付物要求；一键化部署降低环境差异 |

开发环境：Windows 11，Python 3.13（Anaconda），PyTorch 2.11 + CUDA 12.8（GPU 为 RTX 4060 8GB）。

## 3 数据集构建

### 3.1 原始数据集分析

KolektorSDD 初版数据集的构成如下：

| 项目 | 内容 |
|---|---|
| 图像数量 | 399 张灰度图，约 500×1258 像素 |
| 物理件 | 50 个电子换向器，每件约 8 张不重叠表面视图 |
| 类别分布 | 缺陷图 52 张 / 正常图 347 张（明显不平衡） |
| 官方划分 | Train 319 张/ Test 80 张 |
| 语义 | 图像级二分类——有缺陷/ 无缺陷 |

该数据集仅提供像素级掩码，而一级检测需要检测框标注，因此必须进行标注转换。另外，52 张缺陷图明显少于 347 张正常图，类别不平衡问题需要在划分与训练环节专门处理。

### 3.2 数据划分策略

- **沿用官方 Train/Test 划分**：Test 集（80 张）仅在最终评估时使用，训练过程绝不触碰；
- **Train 内部 80/20 再划验证集**：用于模型选择与 GA 适应度评估，分层采样保证缺陷/正常比例一致；
- **物理件级防泄漏**：该数据集中同一物理件（kosXX）的多张子图是同一产品表面的不同视角，缺陷划痕在不同视角间高度相关。若按图像随机划分，同一划痕的不同视角会同时泄漏进训练集与验证集，导致评估指标虚高。因此划分以物理件为单位整体进同一集合，不允许同一 kosXX 跨划分。

### 3.3 数据增强

针对 399 张的小数据规模，采取两层增强策略：

1. **YOLO 训练在线增强**：随机水平翻转、亮度/对比度扰动、仿射变换、mosaic 拼接（由 ultralytics 训练管线提供），提高定位模型的泛化能力；
2. **SVM 负样本增强**（M3）：SVM 的正负样本严重不平衡且正常件纹理与划痕在特征空间易混淆，因此除 YOLO 误检框外，额外从 347 张无缺陷图采样**随机方形块**与**边缘条带块**作为负样本，丰富负样本特征多样性（效果见 4.2.3）。

### 3.4 探索性数据分析（EDA）

训练前通过 `eda.py` 脚本对数据集做探索性分析并输出可视化图表，包括：类别分布统计、缺陷区域数量与面积分布、样本图像及掩码叠加示例等，为后续的 min_area 阈值选择、正负样本设计提供依据。EDA 图表存于 `backend/artifacts/eda/`。

---

## 4 三个技术方向的详细设计

### 4.1 计算机视觉方向：YOLOv8 缺陷区域定位

#### 4.1.1 任务定义与模型选择

一级检测的任务是**高召回地定位所有可疑缺陷区域**：即使多检也不能漏检，因为漏检无法在下游补救，而误检可由二级 SVM 过滤。

模型选择 **YOLOv8n**（nano，约 3.2M 参数）：轻量、训练快、部署方便，8GB 显存与课程环境均无压力；且本课题的架构贡献在于两级分工，更换更大检测模型后二级判别架构保持不变，具备可迁移性。

#### 4.1.2 训练配置

| 配置项 | 取值 |
|---|---|
| 模型 | yolov8n |
| 输入尺寸 | imgsz=640（rect 矩形训练） |
| 训练轮数 | 100 epochs |
| 批量大小 | 16 |
| 优化器 | 自动（SGD/Adam 按 ultralytics 默认） |
| 增强 | 翻转/亮度/仿射/mosaic（应对小数据集） |
| 设备 | GPU（RTX 4060，CUDA 12.8，约 2~5 分钟完成） |

训练产物：`backend/weights/` 下的 `.pt` 权重文件；训练曲线与评估图表存于 `backend/runs/`。

#### 4.1.3 评估结果

- **验证集 mAP50 ≈ 0.46**（0.4614）；
- **官方 Test 集**：mAP50 ≈ 0.03，**Recall ≈ 0.9**。

Test 集 mAP50 很低而 Recall 高达 0.9，直观反映了小数据集训练的 YOLO 置信度标定差的问题——但 Recall 0.9 恰恰说明"高召回定位"的一级设计目标达成：九成缺陷区域都能被候选框覆盖，精度交给二级判别。

#### 4.1.4 置信度标定诊断与高召回策略

开发阶段通过诊断脚本（`diag_conf.py`，输出置信度分布与诊断图 `backend/artifacts/diag_part0.png`）发现：训练后模型输出的候选框置信度普遍低于 0.02，单图约 300 个低分候选框中最高仅 0.013。若按常规阈值（如 0.25）过滤，几乎所有缺陷都会被过滤掉；若把阈值压到 0.001 直接输出，又会带来海量误检。由此确定两级架构的两个工

### 4.2 机器学习方向：SVM 二级判别

#### 4.2.1 特征工程

对 YOLO 输出的每个候选框（统一缩放至 64×64），提取三类特征，拼接为 **1826 维**特征向量：

| 特征 | 维数 | 说明 |
|---|---|---|
| HOG 梯度直方图（手写实现） | 1764 | 64×64 图像，cell 8×8，9 方向梯度直方图，2×2 块内 L2 归一化，块步长 1：7×7 块 × 4 cell × 9 bins = 1764 维。描述缺陷边缘的**形状/方向**信息（划痕具有明显的方向性） |
| LBP 局部二值模式（旋转不变均匀模式） | 59 | LBP^riu2 描述局部**纹理**信息，对光照不敏感，可区分划痕与正常加工纹理 |
| 统计特征 | 3 | 灰度均值、灰度标准差、梯度幅值均值，补充整体亮度与边缘强度信息 |

HOG 采用 numpy 手写实现（Sobel 梯度 → 9 方向 cell 直方图 → 块内 L2 归一化），保证答辩时能讲清原理，也便于修改参数。

#### 4.2.2 正负样本构建

- **正样本**：与真值框 IoU ≥ 0.5 的 YOLO 候选框（真缺陷区域）；
- **负样本**（三类来源）：
  1. YOLO 误检框（与真值框 IoU < 0.5 的候选）；
  2. 正常件**随机方形块**采样；
  3. 正常件**边缘条带块**采样（正常件的端面边缘、加工纹理与划痕在特征空间易混淆，是假阳性的主要来源）。

#### 4.2.3 训练与评估

采用 scikit-learn 的 SVC：**RBF 核 + class_weight='balanced'**（应对正负样本不平衡）。评估结果：

- **验证集分类 AUC ≈ 0.99**：候选框级别的缺陷/误检判别能力很强；
- **正常件假阳性从 7 框降至 1 框**：负样本增强（随机块 + 边缘条带块）是关键改进——修订前正常件平均产出 7 个假阳性框，补充负样本后降至 1 框，再经 GA 优化阈值后基本消除。

### 4.3 智能优化方向：遗传算法超参数优化

#### 4.3.1 优化问题定义

两级流水线中有多个强相关的可调参数：SVM 的超参数（C、γ、核函数）与两级决策阈值（YOLO conf 阈值、SVM 概率阈值）共同决定端到端性能，孤立调参无法找到全局最优组合。GA 对 **5 维基因**联合寻优：

#### 4.3.2 遗传算子设计（手写实现）

| 算子 | 设计 |
|---|---|
| 种群初始化 | 均匀随机（各基因范围内），种群规模 16~30（实际运行取 16） |
| 选择 | 锦标赛选择，k=3 |
| 交叉 | 单点交叉，Pc=0.8 |
| 变异 | 混合变异，Pm=0.1：连续基因高斯扰动（σ=0.15×取值范围）、离散基因（核函数）重采样 |
| 精英保留 | 每代保留最优 2 个个体直接进入下一代 |
| 终止条件 | 迭代 20 代（设计 50 代，按运行时间实测调整） |

#### 4.3.3 适应度函数与加速策略

**适应度 = 验证集两级流水线的图像级 F1**：直接以端到端性能为目标，而非孤立的 SVM 准确率，保证寻优方向与系统最终目标一致。

加速设计：HOG/LBP 特征向量一次性缓存到磁盘，GA 每代个体只需训练 SVM + 预测，秒级完成，20 代 × 16 个体在 10~20 分钟内可完成；工程上采用多进程并行（n_jobs=12）进一步缩短运行时间。

**输出**：收敛曲线图、最优参数 JSON，写入 `model_metadata` 与 `ga_history` 表，推理时由后端加载。

#### 4.3.4 对比实验

为证明 GA 相对"人工调参"的增益，设计了三种方法的对比实验：

| 方法 | 搜索规模 | 说明 |
|---|---|---|
| 随机搜索 | 60 组随机参数 | 无启发式信息，作为弱基线 |
| 网格搜索 | 关键参数固定阈值基线 | 人工调参的典型做法 |
| 遗传算法 | 16 个体 × 20 代 | 本课题方法 |

对比图 `ga_comparison.png` 显示 GA 的验证集 F1 显著优于随机搜索与网格搜索基线，且收敛曲线表明种群在约第 6 代起开始收敛（最优 F1 约 0.60 并稳定），验证了智能优化方向的实际价值。

### 4.4 两级流水线整体评测

以图像级指标（每张图最终判定是否正确）对完整流水线做端到端评测（`eval_pipeline.py`）：

| 指标 | 全候选直接判定（无 SVM） | 两级流水线（GA 优化后） |
|---|---|---|
| 图像级 Precision | ≈ 0.17 | ≈ 0.75 |
| 图像级 F1 | ≈ 0.28 | ≈ 0.67（Test 0.6667） |

一级定位的高召回（Test Recall≈0.9）与二级判别的强区分能力（AUC≈0.99）在 GA 联合优化的参数组合下协同工作，端到端 F1 提升约 2.4 倍，两级架构的有效性得到验证。

---

## 5 界面设计

### 5.1 总体布局与设计风格

前端基于 **Vue3 + Vite + Element Plus + ECharts** 构建，整体为**侧边栏导航 + 顶栏标题**布局，深色工业风配色，与工业质检系统的定位一致。四个页面通过侧边栏切换：在线检测、检测历史、统计概览、模型信息。

交互流程：拖拽或点击上传图片（Element Plus 上传组件）→ 前端本地预览 → 点击「开始检测」调用 `POST /api/v1/detect` → 返回结果后在预览图上用 **canvas 叠加渲染缺陷框**（红框 + 置信度标签）→ 结果卡片展示整体判定（有缺陷/无缺陷）、缺陷数量、平均置信度与处理耗时。

### 5.2 检测历史页（/history）

- Element Plus **表格**展示历史检测记录：缩略图、文件名、判定结果（标签着色）、缺陷数、置信度、检测时间；
- 顶部**筛选栏**：按判定结果（全部/有缺陷/无缺陷）过滤；
- **分页**：服务端分页（?page&size）；
- 点击行打开**详情对话框**：大图 + 复现标注框 + 完整检测信息；
- 支持**删除**记录。

### 5.3 统计概览页（/stats）

- **KPI 卡片**：累计检测数、缺陷率、今日检测数；
- **近 7 天检测趋势折线图**（ECharts）；
- **缺陷数分布柱状图**（ECharts）。

### 5.4 模型信息页（/models）

- 三个模型（YOLOv8 / SVM / GA）的**版本与指标卡片**：训练时间、评估指标（mAP50、AUC、F1 等）；
- **GA 收敛曲线图**；
- **GA 最优参数表**：C、γ、核函数、conf_th、prob_th。

## 6 调试

### 6.1 调试方法与工具

- **分层调试**：算法脚本 → 数据库 → 后端 API → 前端页面，逐层独立验证后再联调；
- **诊断脚本**：针对可疑问题编写一次性诊断脚本（如置信度分布统计 `diag_conf.py`），用数据说话；
- **Swagger 冒烟**：后端接口在 http://localhost:8000/docs 逐一手工冒烟验证，再与前端联调；
- **pytest 单元测试**：数据层与 API 层均有自动化测试覆盖；
- **日志与监控**：GA 等长任务 stdout 直写日志文件（`ga_run.log`），避免管道断开导致进程死亡；后台任务用独立进程运行并挂载自动收尾监控。

### 6.2 典型问题定位与解决（6 例实录）

| # | 问题现象 | 定位过程 | 解决方案 |
|---|---|---|---|
| 1 | YOLO 候选框置信度普遍 < 0.02，最高仅 0.013，任何阈值都难以兼顾召回与精度 | 编写诊断脚本统计全测试集置信度分布 | 确立两级架构：conf_th=0.001 + 每图 top-50 候选送入 SVM 二级判别（方案核心，见 2.3） |
| 2 | 正常件检出 7 个假阳性框 | 可视化假阳性区域，发现集中在端面边缘与加工纹理 | SVM 负样本增强：随机块 + 边缘条带块采样 → 假阳性降至 1 框，GA 优化阈值后基本消除 |
| 3 | GA 后台任务两次中断（BrokenPipeError） | 后台任务管道被会话切换关闭，主进程 print 抛异常死亡 | stdout 直写日志文件 + nohup 完全独立进程运行，根治管道依赖 |

### 6.3 调试小结

调试过程的最大体会是**"用诊断数据驱动方案决策"**：置信度标定差这一核心问题的发现（而非猜测）直接催生了两级架构；假阳性问题通过可视化定位到正常件端面/纹理，进而设计针对性负样本。每个问题的现象、定位、解决均记录在案，保证了开发过程可追溯。

---

## 7 总结与展望

### 7.1 工作总结

本课程设计完成了《基于深度学习的轴承表面缺陷智能检测系统》的全部开发工作：

1. **数据构建**：完成 KolektorSDD 数据集的掩码→bbox 标注转换、物理件级防泄漏划分、分层验证集划分与 EDA 分析；
2. **三方向算法**：① YOLOv8n 一级定位（计算机视觉）；② HOG+LBP+统计特征 SVM 二级判别（机器学习）；③ 手写遗传算法联合优化 SVM 超参数与两级阈值（智能优化），并完成随机搜索/网格搜索对比实验；
3. **B/S 应用系统**：FastAPI 后端 + Vue3/Element Plus/ECharts 前端四页面 + PostgreSQL 持久化 + Redis 缓存，Docker Compose 一键部署；
4. **工程文档**：规格说明书、设计说明书、README、答辩材料（PPT/PDF/逐字稿）齐备，开发全程留痕（AI 使用记录）。

### 7.2 最终性能指标汇总

| 模块 | 指标 | 结果 |
|---|---|---|
| YOLOv8n 一级定位 | 验证集 mAP50 | ≈ 0.46（0.4614） |
| YOLOv8n 一级定位 | 官方 Test 集 Recall | ≈ 0.9（高召回设计） |
| SVM 二级判别 | 验证集 AUC | ≈ 0.99 |
| 两级流水线 | 图像级 Precision | 0.17 → ≈ 0.75 |
| 两级流水线 | 图像级 F1 | 0.28 → ≈ 0.67（Test 0.6667） |
| GA 优化 | 收敛速度 | 约第 6 代起收敛稳定 |
| 工程 | 单元测试 | 17 个全部通过 + 端到端冒烟 |

### 7.3 不足与反思

1. **数据量小**：仅 52 张缺陷图，模型泛化能力受限；置信度标定差问题只能以两级架构工程补救，未能从根本上解决；
2. **数据集非轴承本体**：KolektorSDD 拍摄对象为换向器表面，虽方法可迁移，但未在真实轴承图像上验证；
3. **定位精度有限**：Test 集 mAP50 仅 0.03，候选框定位质量不高，靠 SVM 特征判别兜底。

### 7.4 展望

1. **数据层面**：采集真实轴承表面缺陷图像扩充训练集，或采用自监督预训练、少样本学习缓解小样本问题；
2. **模型层面**：一级检测升级为更强的检测模型（更大规模的 YOLO 版本或 Transformer 系检测器），定位质量提升后两级架构保持不变；
3. **系统层面**：接入工业相机实现在线实时检测，扩展多类缺陷细粒度分类，并将系统部署到真实产线验证。

## 附录 ：参考文献

[1] Tabernik D, Šela S, Skvarč J, Skočaj D. Segmentation-based deep-learning approach for surface-defect detection[J]. Journal of Intelligent Manufacturing, 2020, 31(3): 759-776.

[2] KolektorSDD: Kolektor Surface-Defect Dataset[EB/OL]. University of Ljubljana. https://www.vicos.si/Downloads/KolektorSDD.

[3] Jocher G, Chaurasia A, Qiu J. Ultralytics YOLOv8[EB/OL]. https://docs.ultralytics.com.

[4] Cortes C, Vapnik V. Support-vector networks[J]. Machine Learning, 1995, 20(3): 273-297.

[5] Dalal N, Triggs B. Histograms of oriented gradients for human detection[C]. IEEE CVPR, 2005: 886-893.

[6] Ojala T, Pietikäinen M, Mäenpää T. Multiresolution gray-scale and rotation invariant texture classification with local binary patterns[J]. IEEE TPAMI, 2002, 24(7): 971-987.

[7] Holland J H. Adaptation in Natural and Artificial Systems[M]. Ann Arbor: University of Michigan Press, 1975.
