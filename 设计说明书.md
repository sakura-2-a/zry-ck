基于深度学习的轴承表面缺陷智能检测系统 设计说明书
课程名称：《制造智能技术》课程设计
学生姓名：张芮源
学号：202412020125
指导教师：董旭彬
完成日期：2026 年 9 月
数据来源：KolektorSDD 公开工业表面缺陷数据集

1 背景
轴承表面如果有划痕、裂纹等缺陷，会影响设备寿命，严重时还会造成停机。现在工厂里很多检测还是靠人工目检，效率低，不同人判断标准也不一样，时间长了还容易漏检。传统机器视觉方法一般要人工设计特征，换光照、换纹理后效果就不稳定。

深度学习可以自动学特征，所以本设计用深度学习做表面缺陷检测。数据集用 KolektorSDD，共 399 张灰度图，其中 52 张有缺陷、347 张正常，提供像素级掩码标注。

2 总体方案
系统主要功能包括：上传图片检测、显示缺陷框、保存检测历史、查看统计信息、查看模型信息、离线训练和调参。

技术选型：

YOLOv8：定位缺陷候选区域；

SVM：对候选框做二次判别；

遗传算法：优化 SVM 参数和检测阈值；

FastAPI：后端；

Vue3 + Element Plus + ECharts：前端；

PostgreSQL：保存检测记录；

Redis：缓存；

Docker：部署。

整体思路是：YOLO 先尽量多找候选框，不怕误检；SVM 再判断哪些是真缺陷；GA 负责调参数，让整条流水线效果更好。

3 数据集处理
KolektorSDD 只有像素掩码，没有检测框，所以先把掩码转成 bbox。数据划分沿用官方 Train/Test，Test 只在最后评估时使用。训练集内部再按 80/20 分验证集。

划分时按物理件划分，不按单张图随机分，因为同一个物理件的多张图是同一产品不同视角，如果随机分会导致信息泄漏，指标会虚高。

增强方面：

YOLO 训练时用翻转、亮度对比度扰动、仿射变换、mosaic；

SVM 负样本额外加入正常件的随机方形块和边缘条带块。

另外用 EDA 脚本统计类别分布、缺陷面积等，辅助后面选阈值。

4 三个技术方向
4.1 YOLOv8 缺陷定位
用 YOLOv8n，输入 640，训练 100 轮，batch 16，用 GPU 训练。

结果：

验证集 mAP50 约 0.46；

官方 Test 集 mAP50 约 0.03，但 Recall 约 0.9。

诊断发现，YOLO 输出的候选框置信度普遍很低，最高只有 0.013 左右。按常规阈值 0.25 过滤，缺陷基本都会被滤掉。所以改成 conf_th=0.001，每张图取 top-50 候选框，交给 SVM 二级判别。

4.2 SVM 二级判别
每个候选框缩放到 64×64，提取三类特征：

HOG：1764 维，手写实现；

LBP：59 维；

统计特征：灰度均值、标准差、梯度均值，共 3 维。

合计 1826 维。

正样本：和真值框 IoU≥0.5 的 YOLO 候选框。
负样本：YOLO 误检框、正常件随机块、正常件边缘条带块。

分类器用 SVC，RBF 核，class_weight='balanced'。验证集 AUC 约 0.99。正常件假阳性从平均 7 个框降到 1 个，GA 调阈值后基本消除。

4.3 遗传算法优化
优化 5 个参数：SVM 的 C、gamma、核函数，以及 YOLO conf 阈值、SVM prob 阈值。

GA 为手写实现：

种群规模 16；

锦标赛选择，k=3；

单点交叉，Pc=0.8；

变异 Pm=0.1；

精英保留 2 个；

迭代 20 代。

适应度用验证集图像级 F1。HOG/LBP 特征提前缓存，GA 每代只训练 SVM 和预测，速度较快。和随机搜索、网格搜索对比，GA 的 F1 更好，大约第 6 代开始收敛。

4.4 整体效果
无 SVM 时，图像级 Precision 约 0.17，F1 约 0.28。
加入 SVM 和 GA 后，Precision 约 0.75，F1 约 0.67。说明两级流水线确实有效。

5 界面设计
前端用 Vue3，一共四个页面：

在线检测：上传图片，canvas 画缺陷框，显示有无缺陷、缺陷数量、平均置信度、耗时；

检测历史：表格展示记录，支持筛选、分页、查看详情、删除；

统计概览：累计检测数、缺陷率、今日检测数、近 7 天趋势、缺陷数分布；

模型信息：YOLO/SVM/GA 的指标、GA 收敛曲线、最优参数。

6 调试
调试按层进行：算法脚本、数据库、后端 API、前端页面。

主要问题：

YOLO 置信度太低。写诊断脚本看分布后，确定用两级架构，低阈值取候选框，SVM 再过滤。

正常件假阳性多，集中在边缘和加工纹理。增加随机块和边缘条带负样本后减少。

GA 后台任务因管道断开中断。改成 stdout 写日志文件，并用独立进程运行。

最后用 pytest 做单元测试，Swagger 做接口冒烟测试。

7 总结
完成的工作：

KolektorSDD 掩码转 bbox、数据划分、防泄漏处理；

YOLOv8 一级定位；

HOG+LBP+统计特征 SVM 二级判别；

手写遗传算法调参；

FastAPI + Vue3 + PostgreSQL + Redis + Docker 的 Web 系统。

最终指标：

YOLO 验证集 mAP50 约 0.46；

YOLO Test 集 Recall 约 0.9；

SVM 验证集 AUC 约 0.99；

两级流水线图像级 F1 从 0.28 提升到约 0.67。

不足：

缺陷图只有 52 张，数据量小；

KolektorSDD 不是轴承本体，方法可迁移但未在真实轴承上验证；

YOLO 定位精度有限。

以后可以采集真实轴承图像，换更强检测模型，并接入工业相机做在线检测。

参考文献
[1] Tabernik D, 等. Segmentation-based deep-learning approach for surface-defect detection. Journal of Intelligent Manufacturing, 2020.
[2] KolektorSDD 数据集. University of Ljubljana.
[3] Ultralytics YOLOv8 文档.
[4] Cortes C, Vapnik V. Support-vector networks. Machine Learning, 1995.
[5] Dalal N, Triggs B. Histograms of oriented gradients for human detection. CVPR, 2005.
[6] Ojala T, 等. Multiresolution gray-scale and rotation invariant texture classification with local binary patterns. IEEE TPAMI, 2002.
[7] Holland J H. Adaptation in Natural and Artificial Systems. 1975.